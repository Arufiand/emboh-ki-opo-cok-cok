// lib/core/constants/time_constants.dart
class TimeConstants {
  static const Duration API_TIMEOUT = Duration(seconds: 30);
  static const Duration SPLASH_SCREEN_DURATION = Duration(seconds: 2);
  static const Duration SNACKBAR_DURATION = Duration(seconds: 3);
}
